# Agent Guide

The canonical AI collaboration contract now lives in `AGENTS.md`.

Use `AGENTS.md` as the source of truth for agent behavior, documentation flow, verification rules, and task handling.

This file remains only as a compatibility pointer.
